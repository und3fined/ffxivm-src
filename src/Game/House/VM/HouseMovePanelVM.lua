---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by skysong.
--- DateTime: 2025/5/15 10:55
---

local LuaClass = require("Core/LuaClass")
local UIViewModel = require("UI/UIViewModel")


local LSTR =_G.LSTR

local HouseMovePanelVM = LuaClass(UIViewModel)

function HouseMovePanelVM:Ctor()
    self.Height = 0
    self.Rotate = 0
    self.HeightSlider = 0
    self.RotateSlider = 0
    self.HeightViewVisibility = 0
    self.RotateViewVisibility = 1
    self:SetNoCheckValueChange("HeightSlider", true)
    self:SetNoCheckValueChange("RotateSlider", true)
end

function HouseMovePanelVM:OnInit()

end

function HouseMovePanelVM:OnBegin()

end

function HouseMovePanelVM:OnEnd()

end

function HouseMovePanelVM:OnShutdown()

end

function HouseMovePanelVM:SetHeight(Value, bIgnoreSlider)
    self.Height = Value
    if bIgnoreSlider ~= true then
        self.HeightSlider = Value
    end
end

function HouseMovePanelVM:SetRotate(Value, bIgnoreSlider)
    self.Rotate = Value
    if bIgnoreSlider ~= true then
        self.RotateSlider = Value
    end
end
return HouseMovePanelVM